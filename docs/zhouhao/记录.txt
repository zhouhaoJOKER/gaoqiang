主表
1、接单日期户还有合同编号
2、客户原始名称（可以去掉）
3、客户交期（类似备注）
4、跟紧日期（类似日期到货备注）
5、订单金额（需要明细表汇总）
7、拼音码（暂时去掉）

明细表
1、产品id，父明细id，是否主产品（是否存在父子关系，需要确认）
2、明细表中的创建人ID去掉


待确认：
1、在生产订单中选择产品的时候，没有带出具体的sku
2、外跟单中的产品sku中的（产品的单价是否需要跟着客户或者跟着主表上的价格项）
